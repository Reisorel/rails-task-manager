<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed ? "Completed" : "Not completed yet you dumbass" %></p>

<%= link_to "Back", tasks_path %>
<%= link_to "Edit", edit_task_path(@task)%>
<%= link_to "Remove mon ami", task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
